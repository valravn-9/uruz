import { View, Text } from 'react-native';

const TwoScreen = () => {
  return (
    <View className="items-center justify-center flex-1 bg-white dark:bg-black">
      <Text className="color-black dark:color-white">Two</Text>
    </View>
  );
};

export default TwoScreen;
