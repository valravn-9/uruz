import { View, Text } from 'react-native';

const OneScreen = () => {
  return (
    <View className="items-center justify-center flex-1 bg-white dark:bg-black">
      <Text className="color-black dark:color-white">OneScreen</Text>
    </View>
  );
};

export default OneScreen;
